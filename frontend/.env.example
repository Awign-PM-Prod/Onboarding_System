# Leave empty in local Vite to use the proxy (/api -> http://localhost:8089).
# If set, use the backend origin only (8089), not the Vite port (8088).
# Production Docker: leave empty so the browser uses same-origin /api via nginx
# under https://staffing-portal.awignhub.in
VITE_API_BASE_URL=
