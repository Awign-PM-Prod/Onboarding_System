VITE_SUPABASE_URL=https://<project-ref>.supabase.co
VITE_SUPABASE_ANON_KEY=<anon-key>
# Leave empty in local dev to use Vite proxy (/api -> http://localhost:8089).
# If set, use the backend port (8089), not the Vite dev server (8088).
VITE_API_BASE_URL=http://localhost:8089
